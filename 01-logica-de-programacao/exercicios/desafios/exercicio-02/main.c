#include <stdio.h>

int main(int argc, char const *argv[])
{
    int nota1 = 10;
    int nota2 = 15;

    int media = (nota1 + nota2) / 2;

    printf("A média das notas %d e %d foi de %d!\n", nota1, nota2, media);
    return 0;
}
